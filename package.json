{
  "name": "stack-ci-gate",
  "private": true,
  "version": "1.0.1",
  "description": "CI optimizer for PRs on top of other PRs. Skip mid-stack CI. Keep the next to merge.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test src/*.test.mjs .github/scripts/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/raulgg/stack-ci-gate"
  },
  "license": "MIT"
}

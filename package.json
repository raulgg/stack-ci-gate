{
  "name": "stack-ci-gate",
  "private": true,
  "version": "1.0.1",
  "description": "Skip extra CI when PRs are stacked. Run it on the next to merge, and the last in the stack.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test src/*.test.mjs .github/scripts/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/raulgg/stack-ci-gate"
  },
  "license": "MIT"
}

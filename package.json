{
  "name": "stack-ci-gate",
  "private": true,
  "version": "1.0.1",
  "description": "Skip redundant CI on stacked pull requests. Run remaining-bottom and optionally the top.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test src/*.test.mjs .github/scripts/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/raulgg/stack-ci-gate"
  },
  "license": "MIT"
}

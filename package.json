{
  "name": "stack-ci-gate",
  "private": true,
  "version": "1.0.1",
  "description": "Optimize CI on GitHub stacked pull requests. Skip mid-stack; run the top and bottom of the stack.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test src/*.test.mjs .github/scripts/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/raulgg/stack-ci-gate"
  },
  "license": "MIT"
}

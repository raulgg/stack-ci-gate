{
  "name": "stack-ci-gate",
  "private": true,
  "version": "1.0.1",
  "description": "When PRs stack on each other, skip CI in the middle. Run it on the next to merge, and the last if you want.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test src/*.test.mjs .github/scripts/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/raulgg/stack-ci-gate"
  },
  "license": "MIT"
}

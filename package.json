{
  "name": "stack-ci-gate",
  "private": true,
  "version": "1.0.1",
  "description": "A CI gate for GitHub stacked pull requests. Choose to run only the top and bottom of the stack to optimize CI.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test src/*.test.mjs .github/scripts/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/raulgg/stack-ci-gate"
  },
  "license": "MIT"
}

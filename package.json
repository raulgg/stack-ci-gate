{
  "name": "stack-ci-gate",
  "private": true,
  "version": "1.0.1",
  "description": "A CI gate for GitHub stacked pull requests. Always run the bottom of the stack, optionally the top, to optimize CI.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test src/*.test.mjs .github/scripts/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/raulgg/stack-ci-gate"
  },
  "license": "MIT"
}

{
  "name": "stack-ci-gate",
  "private": true,
  "version": "1.0.1",
  "description": "When pull requests are stacked on each other, skip CI in the middle of the chain. Run it on the next one to merge, and on the last one if you want.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test src/*.test.mjs .github/scripts/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/raulgg/stack-ci-gate"
  },
  "license": "MIT"
}
